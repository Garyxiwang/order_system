{
  "name": "order-system-api",
  "build": {
    "commands": [
      "pip install -r requirements.txt"
    ]
  },
  "start": "uvicorn main:app --host 0.0.0.0 --port $PORT",
  "environment": {
    "PYTHON_VERSION": "3.9"
  }
}